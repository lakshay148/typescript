let apples: number = 1;

let title: string = "lakshay";

let now: Date = new Date();

//array
let colors: string[] = ["red", "green"];

//classes

class Car {}

let myCar: Car = new Car();

//object literal
let point: { x: number; y: number } = {
  x: 10,
  y: 20,
};

//functions
const logNumber: (i: number) => void = (i: number) => {
  console.log(`logging the input number ${i}`);
};
